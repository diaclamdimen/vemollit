failed_checks = []  # Ensure failed_checks is initialized as a list if it's not already

# Assuming model_id is defined somewhere earlier
model_id = 123  # Replace with the actual value of model_id

failed_checks.append(model_id)
